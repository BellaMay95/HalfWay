import React, { Component } from 'react';
import { render } from 'react-dom';

export default class ProfileChanges extends Component {
    render() {
        return (<p>Hello! I'm the ProfileChanges Component!</p>);
    }
}