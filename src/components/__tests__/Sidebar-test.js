import React from 'react';
import { mount } from 'enzyme';

import Sidebar from '../Sidebar';

describe('Sidebar', () => {
    it("2+2 = 4", () => {
        let math = 2 + 2;
        expect(math).toBe(4);
    });
});