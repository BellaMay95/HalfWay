import React, { Component } from 'react';
import { render } from 'react-dom';

export default class CreateAccount extends Component {
    render() {
        return (<p>Hello! I'm the CreateAccount Component!</p>);
    }
}